@model IEnumerable<Model.EF.LoaiKhachHang>

@{
    ViewBag.Title = "Loại khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
}

<h2>Quản lý loại khách hàng</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaLoaiKhachHang }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaLoaiKhachHang }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaLoaiKhachHang })
        </td>
    </tr>
}

</table>
